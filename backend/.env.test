TEST_DB_HOST=localhost
TEST_DB_PORT=3307
TEST_DB_NAME=live_auction_test
TEST_DB_USER=root
TEST_DB_PASSWORD=testpass
TEST_REDIS_HOST=localhost
TEST_REDIS_PORT=6380

# Test environment ports (separate from dev 3001/5173)
PORT=3002

# Standard env vars consumed by src/config/env.ts — ensures the WS integration
# tests connect to the same Redis instance as the factory seed helpers.
DB_HOST=localhost
DB_PORT=3307
DB_USER=root
DB_PASSWORD=testpass
DB_NAME=live_auction_test
REDIS_URL=redis://localhost:6380
